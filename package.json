{
  "name": "hyperaion-lp",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "build:deploy": "vite build && mkdir -p dist/hyperaion/lp && cp -r dist/assets dist/hyperaion/lp/ && cp dist/index.html dist/hyperaion/lp/ && rm -rf dist/assets dist/index.html",
    "preview": "vite preview"
  },
  "devDependencies": {
    "vite": "^8.0.1"
  }
}
